# Python
__pycache__/
*.pyc
.venv/
venv/

# Editable install / build artifacts
*.egg-info/
build/
dist/

# Project outputs
output/

# Per-source line-segment cache (developer scratch, not packaged)
lines.json

# Models cached by ultralytics (developer scratch in repo root)
/*.pt
# Bundled production weights inside the package ARE tracked (D-04: shipped as package-data)
!contador_pajaros/weights/*.pt

# OS
Thumbs.db
.DS_Store

# Demo / E2E scratch (NOT part of the package)
var/
*.ts
*.sql
e2e_birds_bet.py
seed_birds_calibrate.py
